{
  "$schema": "https://json-schema.org/draft-07/schema",
  "id": "https://jacklehamster.github.io/dok-schema/core/auxiliary-definition.json",
  "description": "An auxiliary is attached to entities and allows actions to be performed on that entity.",
  "definitions": {
    "AuxiliaryId": {
      "description": "Identifier for this auxiliary. Matches a class name",
      "type": "string",
      "pattern": "^[A-Z]\\w+$"
    }
  },
  "properties": {
    "id": {
      "$ref": "#/definitions/AuxiliaryId"
    },
    "namespace": {
      "description": "Namespace to avoid id collision. Optional",
      "type": "string",
      "pattern": "^[\\w.]+$"
    },
    "dependencies": {
      "description": "Other auxiliaries that this is dependent on.",
      "type": "array",
      "items": {
        "$ref": "#/definitions/AuxiliaryId"
      }
    }
  },
  "additionalProperties": {
    "$ref": "any.json"
  }
}